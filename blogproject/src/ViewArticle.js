import React from 'react'

function ViewArticle(props) {
    
    return (
        <div>
            article view.
            component needs: title/ header, span for image, author and date, body for the article maybe a card, footer two suggested articles
        </div>
    )
}

export default ViewArticle
